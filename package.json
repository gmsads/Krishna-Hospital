{
  "name": "krishna-hospital-management-system",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm run build --prefix frontend",
    "start": "npm start --prefix backend",
    "postinstall": "npm install --prefix frontend && npm install --prefix backend"
  }
}
